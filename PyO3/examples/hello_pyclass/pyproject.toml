[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "hello_pyclass"
requires-python = ">=3.11"
# Sourced from Cargo.toml's package.version by the maturin backend
dynamic = ["version"]
