# Workspace Context

## Environment

- **Platform**: Linux devcontainer (VSCode Dev Container)
- **Container name**: `python_devcontainer-devcontainer-1`
- **Host OS**: Ubuntu (Pop!_OS), user `nikhil`
- **Shell**: `/bin/sh`
- **Package manager**: `uv`

## Port Mappings (host → container)

| Host | Container |
|------|-----------|
| 8000 | 8000      |
| 8080 | 8080      |
| 5001 | 5000      |

## Projects

### my-fapi (`/workspace/my-fapi`)

- **Framework**: FastAPI + uvicorn
- **Python**: >= 3.14
- **Package manager**: uv
- **Layout**: `src` layout — app lives in `src/my_fapi/`
- **Entry point**: `main.py` at project root (runs uvicorn programmatically)
- **App module**: `src/my_fapi/app.py` — defines the FastAPI `app` object
- **Run command**: `uv run main.py` from `/workspace/my-fapi`
- **Access**: `http://localhost:8000` on host

#### Known issues / fixes applied
- `.venv` was created with a stale shebang pointing to `/workspace/python/my-fapi/.venv/bin/python` — fixed to `/workspace/my-fapi/.venv/bin/python`
- `uvicorn` and `fastapi` scripts in `.venv/bin/` were missing execute permissions — fixed with `chmod +x`
- `src/my_fapi/app.py` was empty — added minimal FastAPI app with a `GET /` route
- Old uvicorn background processes bind to `localhost` instead of `0.0.0.0`, blocking host access — kill with `kill <PID>` (use `lsof -i :8000` to find PIDs); `fuser` is not installed in this container
- Correct run command: `uv run uvicorn my_fapi:app --app-dir src --host 0.0.0.0 --port 8000`

#### Git config
- Git commits were showing author "Mitali Pagote" — fix by setting global git identity:
  ```bash
  git config --global user.name "Nikhil"
  git config --global user.email "your@email.com"
  ```

### my_flask (`/workspace/my_flask`)

- See project directory for details

## Other Files

- `/workspace/uv-cheatsheet.md` — uv command reference
