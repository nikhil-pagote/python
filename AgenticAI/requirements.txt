# Direct dependencies for the agentic RAG project.
# The workspace is managed with uv (pyproject.toml is the source of truth);
# this file is a convenience for pip:  pip install -r requirements.txt

# Core frameworks
langchain-core==1.4.8
langgraph==1.2.6
langchain-text-splitters==1.1.2

# Providers: reasoning (Groq), embeddings + vector DB (Pinecone)
langchain-groq==1.1.3
langchain-pinecone==0.2.13
pinecone==7.3.0

# Config / API / tooling
python-dotenv==1.2.2
pyyaml==6.0.3
fastapi==0.138.1
uvicorn==0.49.0
pydantic==2.13.4
pytest==9.1.1

# Optional — uncomment + `uv add` when you implement these orchestrators:
# crewai
# google-adk
